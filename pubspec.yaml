name: flutter_card_ui_app_ex01
description: "A new Flutter project."
# pub.dev에 실수로 게시되는 것을 방지 (개인 패키지용)
publish_to: 'none' # pub.dev에 게시하려면 이 줄 제거

# 앱 버전 및 빌드 번호 (형식: 버전+빌드번호)
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# 패키지 의존성
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  riverpod: ^3.1.0
  flutter_riverpod: ^3.1.0
  logger: ^2.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 권장 린트 규칙 (analysis_options.yaml에서 설정)
  flutter_lints: ^6.0.0

# Flutter 패키지 설정
flutter:

  # Material Icons 폰트 포함
  uses-material-design: true

  # 에셋 추가 예시:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # 커스텀 폰트 추가 예시:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
